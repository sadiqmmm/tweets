<div class="panel right">
	<h1>Create A Tweet</h1>
	<%= form_for @tweet do |f| %>
		<%= f.text_area :content, class: "ribbitText", id: "ribbitText" %>
		<%= f.submit "Tweet!" %>
		<span id="charCount">0</span>
	<% end %>
</div>